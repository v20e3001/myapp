<h2>
    <%= link_to 'Add New', '/posts/:id/edit', class: 'header-menu' %>
    My Posts
</h2>
<ul>
    <% @posts.each do |post| %>
        <li>
            <%= link_to post.title, post_path(post) %>
        </li>
    <% end %>
</ul>